fn main() {
    println!("Hello, world!");  // calling println as a macro, instead of a function
}

/* Chapter 19.5 and some hints on macros:
 * "Macros are a way of writing code that writes other code"
 * "Declarative macros (...): macros by example, `macro_rules!` macros"
 */
